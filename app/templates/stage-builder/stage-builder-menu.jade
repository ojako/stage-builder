// output:../../views/stage-builder-menu.html
.btn-group(uib-dropdown)
  button.btn.btn-link(uib-dropdown-toggle) Insert Stage #[span.fa.fa-plus]
  ul.dropdown-menu.list-group.stage-builder-menu
    li.list-group-item(
      ng-click="$ctrl.onAddStage({'type':type})"
      ng-repeat="type in $ctrl.stageTypes track by $index"
      ng-class="type"
    ) #[span(ng-class="type")] {$ type $}
